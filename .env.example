# Ports exposed on the host
WEB_PORT=8080
SERVER_PORT=3000

# The URL the browser should use to reach the API. With the default setup,
# the frontend container (nginx) proxies API requests to the backend.
VITE_API_BASE_URL=http://localhost:8080

# Required: host path to your Core Lightning unix socket (lightning-rpc).
# This is mounted into the backend container at /cln/lightning-rpc.
LNS_CL_RPC_HOST_PATH=/absolute/path/to/lightning-rpc

# Optional server config
LNS_MIN_WITHDRAWABLE_MSAT=1000
LNS_MAX_WITHDRAWABLE_MSAT=100000
LNS_BTC_RPC_URL=http://127.0.0.1:48332
LNS_BTC_RPC_USER=
LNS_BTC_RPC_PASSWORD=
