## Shared dev environment (bitcoind Docker + host lightningd)
#
# This file is meant to be the single source of truth for *development* values used by:
# - `make dev-btc-up`        (Dockerized bitcoind, pinned to Testnet4)
# - `make dev-cln-host`      (host-run lightningd pointing at that bitcoind)
#
# Quick start:
#   1) `cp dev/.env.example dev/.env`
#   2) set `BTC_RPC_USER` and `BTC_RPC_PASSWORD`
#   3) `make dev-btc-up`
#   4) in another terminal: `make dev-cln-host`
#
# Notes:
# - `dev/bitcoin/docker-compose.yml` is pinned to Testnet4 and ignores any chain setting.
# - These values are for local development only. Do not reuse them for deployment.



## Bitcoin Core JSON-RPC (required)
#
# These credentials are used by:
# - bitcoind itself (Docker)
# - your host lightningd (to connect to bitcoind)
BTC_RPC_USER=
BTC_RPC_PASSWORD=

## Bitcoin Core JSON-RPC port (optional)
# Host port published by the dev bitcoind compose.
BTC_RPC_PORT=48332

## Bitcoin Core pruning (optional)
# Pruning keeps only the most recent block data on disk (value is MiB).
#
# LN nodes sometimes need older blocks (for channel/gossip verification). If you see
# CLN logs like "failed to fetch block ... from the bitcoin backend (maybe pruned)",
# increase this value or set it to 0 to disable pruning.
BTC_PRUNE_MB=5500

## Bitcoin Core JSON-RPC host (optional)
# Hostname that host lightningd should use to reach bitcoind.
BTC_RPC_HOST=127.0.0.1



## Core Lightning (host lightningd) settings (optional)
#
# `make dev-cln-host` generates a CLN config and starts lightningd on your host.
CLN_NETWORK=testnet4
CLN_PORT=9735
CLN_ALIAS=cln-host

## Optional: override where dev-cln-host stores state
#CLN_DATA_DIR=/absolute/path/to/dev/.data/host-cln-data
#CLN_RPC_DIR=/absolute/path/to/dev/.data/host-cln-rpc
#CLN_CONF_PATH=/absolute/path/to/dev/.data/host-cln.conf
