## Host-run ln-server environment
#
# This file is loaded by `ln-server` when running on the host (via dotenv).
# It is NOT used by the deployment compose (which uses `deploy/.env`).
#
# Configure the CoreLightning unix socket path.
# For example, if you run `make dev-cln-host` from the repo root, the
# socket will be written to:
#   <repo>/dev/.data/host-cln-rpc/lightning-rpc
#
# Typical host dev:
#   1) `cp dev/.env.example dev/.env` (set BTC creds) + `make dev-btc-up`
#   2) `make dev-cln-host` (runs lightningd on your host)
#   3) `cp server/.env.example server/.env` and set `SERVER_CLN_RPC_PATH` below
#
# Tip: Once lightningd is running, you can verify the socket with:
#   `dev/cln-host/cli.sh getinfo`

## Required: CLN unix socket path
#
# Examples:
# - from `make dev-cln-host`:
#     SERVER_CLN_RPC_PATH=/absolute/path/to/repo/dev/.data/host-cln-rpc/lightning-rpc
# - host-installed CLN default locations vary by OS/network.
SERVER_CLN_RPC_PATH=/path/to/lightning-rpc

## HTTP listener port (optional)
# If you omit this variable entirely, `ln-server` defaults to 3000.
SERVER_PORT=3000

## LNURL-withdraw limits (optional)
# Millisatoshis. These are the min/max values advertised in LNURL-withdraw.
SERVER_MIN_WITHDRAWABLE_MSAT=1000
SERVER_MAX_WITHDRAWABLE_MSAT=100000

## Optional: Bitcoin Core JSON-RPC for /health
#
# If you set BOTH `SERVER_BTC_RPC_USER` and `SERVER_BTC_RPC_PASSWORD`, the server will
# attempt to query bitcoind and include it in `GET /health`.
# (Legacy names `LNS_BTC_RPC_USER`/`LNS_BTC_RPC_PASSWORD` are still accepted.)
#
# For the dev bitcoind (`make dev-btc-up`), this will typically work:
SERVER_BTC_RPC_URL=http://127.0.0.1:48332
SERVER_BTC_RPC_USER=
SERVER_BTC_RPC_PASSWORD=
